<div class="row articles-containter">
    {{#each reviews}}
    <div class="col- articles-single-article">
        <a href="#/home/review/{{authorUid}}{{title}}">
            <div class="single-article-image-containter">
                <div class="single-article-category">
                    {{category}}
                </div>
                <img class="single-article-image" src="{{image}}" />
                <div class="single-article-date">
                    <span>{{month}}</span>
                    <span> {{day}}</span>
                </div>
            </div>

            <div class="single-article-title" title="{{{title}}}">
                {{{title}}}
            </div>

            <div class="single-article-author">
                AUTHOR {{author}}
            </div>

            <div class="single-article-content">
                {{{content}}}
            </div>

            <img class="bottom-line" src="./images/bottom-line.png">
        </a>
    </div>
    {{/each}}
</div>

    {{#if pagination}}
    <div class="row pagination-container no-gutters">
        <div class="d-flex justify-content-start" id="previous">
            <a href="#/home/?pageNumber=1&pageSize={{pageSize}}">«</a>
        </div>

        <div class="col d-flex justify-content-center pages">
            {{#each pageNumbers}}
            <a href="#/home/?pageNumber={{this}}&pageSize={{../pageSize}}">{{this}}</a> {{/each}}
        </div>

        <div class="d-flex justify-content-end" id="next">
            <a href="#/home/?pageNumber={{count}}&pageSize={{pageSize}}">»</a>
        </div>
    </div>
    {{/if}}
</div>